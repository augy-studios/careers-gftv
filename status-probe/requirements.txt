# Careers@GFTV status probe.
#
# One dependency, pinned to a major version the way the bot pins its three: the
# VPS is updated by hand, so a floating patch release is what gets a security
# fix in without a code change, and a floating major release is what breaks the
# probe on a morning nobody was touching it.
#
# The async HTTP client, for the four public GETs a minute and the one RPC that
# records them. Nothing else: no Telethon, no bcrypt, nothing the bot needs.
httpx>=0.27,<1
