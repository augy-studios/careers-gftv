---
title: 工作备忘
summary: 这次构建据以进行的那份备忘，作为快照保存，每一节一页。
---

# next-steps.md

国际兽视入队平台构建的工作备忘，在每一个阶段的开始和结束都会重写一次。**自 2026
年 9 月 8 日起，它住在 `reference/` 里并已进入代码库**；在前十四个阶段里它一直被
git 忽略。说明书是 `reference/careers-gftv-spec.md`，而这个文件绝不凌驾于它之上。

最后一次写入：**在第 14 阶段「说明文件」期间，2026 年 9 月 11 日**，属于第 10g
部分。第 10e 部分把这个文件和说明书移进了 `reference/`，并生成了这份快照所由来的
九个页面。它还把整份备忘重写了一遍，使其通过 `check-copy.js`。第 10f 部分是同一
天里一位读者提出的四件事，分布在机器人和求职网站上。**10e 和 10f 作为一个提交
`64b122c` 一起出去了**，而 10g 是那个提交没有带上的四个备忘页面的华文。

第 1、2 和 2a 部分已作为 `14dc190` 和 `f095bad` 推送，并在线上验证过。**第 2b 和
3 部分一起作为 `12ec9de` 提交，尚未对照部署验证。** **第 4 和 4a 部分一起作为
`7d82b02` 推送，同样尚未对照部署验证。** **第 5 部分，职位发布者指南，已作为
`04261b4` 推送并在线上验证过。** 那二十页对职位发布者正常提供，对陌生人则被拒
绝。偏离 130 的那两条路由对一个真实的发布者会话回答了 403。**第 5a 部分，
`8e197c4`，在运行过 `tests/phase7-test.mjs` 之后把它修好了。** 第 2 节就是这个阶
段；它的部分清单写着每一部分交付了什么。

**第 7 部分是开发者指南，17 页，已作为 `61d9ce5` 推送，尚未对照部署验证。** 在它
之前有四个决定被提出并回答，在它之后有四点顾虑，八项全部按推荐处理。前四个里最大
的那一个把这一部分一分为二。指南是第 7 部分。生成的说明书页面和备忘页面是
**7a**，它现在有了自己一个受限区块可以写进去。它还补上了第 13 阶段决定 6 留下的
那一半：受限页面前置数据里的一个 `data:` 键。一个已提交的 JSON 文件是随页面自己
的响应到达浏览器的，永远不会有自己的地址。这就是为什么 `tests/` 里全部十六个脚本
都在同一页的一个下载按钮后面。**它发现了一件它没有修的事。**
`main-site/README.md` 开头仍写着「Current phase: 6 of 15」，落后了八个阶段，而
README 归第 10 部分管。

**第 8 部分是截图机制和三个发现文件，已作为 `a84170a` 推送，尚未对照部署验证。**
四个决定被提出，其中三个按推荐回答。第四个把发现文件留在第 8 部分里面，而不是拆
成 8a。**它刻意一张图都不拍。** 清单里的 25 个位置、Playwright 脚本、它自己作用域
的 `package.json`、构建的五项拒绝、`robots.txt`、`sitemap.xml` 和 `llms.txt` 都落
在这里。截图运行本身需要一个播了种的数据库和一个员工会话，那是决定 27 那次手动的
坐下来。**偏离 132** 就是那次运行偏离 16g 的地方。只有一个数据库，所以「绝不针对
生产环境」守不住。取而代之的是一个脚本，除非职位板上正显示着播种的职位，否则它拒
绝启动。

**第 9 部分是为全部 82 页做的华文。** 它已作为 `c0be461` 推送，并在 2026 年 9 月
7 日对照部署验证过。`/api/content?path=/portal/applying&locale=zh` 回答的是华文的
标题、摘要和正文，而 `/api/nav?locale=zh` 回答的是翻译过的区块标题。**所以那次部
署确实带着数据库跑过，并把两张表都填上了。** 这是第 9 部分自己没法证明的那一件
事。下面那句关于「什么都还没写进去」的话，现在是历史，而不是一个状态。镜像和视图
都是活的，而这正是第 10 部分的 `/docs` 所读的东西。

**2026 年 9 月 7 日，一位读者在线上发现了一个缺陷。** 它已经修好并作为
`e86f53b`、「phase 14 part 9a」推送，尚未对照部署验证。在说明文件的侧栏里展开一个
区块，然后切换语言，区块标题就不再响应，直到手动重新加载页面为止。**它本来要跟着
第 10 部分走，结果没有。** 按当时的样子提交整棵树，意味着它自己单独落地了。一个
单独落地的提交，没法用一个还不存在的部分来命名。所以它取了它落在后面的那个部分之
后的字母，而它的说明写在下面第 10 部分底下。简短版是：`drawSidebar` 每次绘制时都
会再加一次它那个代理点击处理器。挂载点比重绘活得久，于是两个处理器让一次点击把区
块切换了两遍。同样的形状在它旁边的 `drawAccount` 里也找到了，作用在 `document`
上，并与它一起修好。

**2026 年 9 月 7 日，在第 10 部分开始之前定下了四个决定，四个都按推荐处理。** 第
**7a 部分排在第 10 部分之前**。第 10 部分**拆成 10 和 10a**，先是机器人，再是文
字。**第 30 项那份「离开这个构建的写入」清单在收尾里做出来。** 第 11 阶段的**表单
回执通知落在 10a 之后、翻转之前**，放在它自己的提交里。所以这个阶段剩下的顺序是
7a、10、10a、通知，然后是翻转。网站的两半都在里面，加上从来没有存在过的那套管路。
那是迁移 `042` 的两张表和它们之上的视图、构建的数据库连接，以及每种语言一份搜索索
引。读取路径也在里面，还有那条放在没人翻译过的页面上的提示。**四个决定被提出，其
中一个没有按推荐走。** 这一部分曾被提议为 9 加一个 9a，而答案是一次全做完。**迁移
`042` 已于 2026 年 9 月 6 日手动应用。**

**视图是这支迁移里最值得一读的部分。** 它把翻译表内连接到公开镜像上，所以一个受限
页面的华文连接不到任何东西，也就无法出现。Telegram 机器人的 `/docs` 读它，自己不
带任何层级判断。这**解除了这个文件从 9 月 3 日起一直背着的那份担心**：层级规则被
实现两次，而两份副本可能不一致。现在没有第二份副本。

**两条偏离，133 和 134。** 第一条是华文在哪里书写：写在文件里，而表是构建写出的一
份副本。这违反 16e 的字面，却服务于它的目的。第二条是一个可为空的列。Vercel 是浅
克隆的，而在那里加 `not null` 会让一次本来正确的部署失败。

**目前还没有任何东西被写进数据库。** 这台机器上没有 `.env.local`，所以每一次构建
都是 `--no-database`。两张表已经建好且是空的，而第一次部署会把它们填上。在那之前，
一位华文读者拿到的是带着提示的英文。这和读取路径在数据库连不上时做的是同一件事。

**它在英文里发现了两处过时并把两处都修好了**，因为正是第 9 部分让它们过时的。员工
索引页开头仍写着「These guides are in English today」。而 **3a 仍说说明文件网站的
员工那一半保持英文**，这一点 16f 已在 9 月 3 日推翻，却没有人回头去改。

**它发现 `/staff` 从来没有被挡在搜索索引之外。** 完全没有 `robots.txt`，没有
`X-Robots-Tag`，而每一个受限地址都用外壳回答 200。这不算泄露，因为那个回答不带任
何内容，而这里用两样工具把它关上了。

**在它之后提出了四点顾虑，四点都按推荐回答。** 其中改动了代码的那一点，给脚本加了
一个 `--dry-run`。**它现在已经跑过了**：一张真实的、65 KB 的登录页 webp，2026 年
9 月 5 日拍进一个临时目录。这是这一整套东西第一次启动浏览器。在那之前再读一遍代码
又发现了两个缺陷：每一张遮罩本来都会是洋红色，而建议图层根本不会被画出来。还有一
个是跑起来才发现的：对求职网站那些居中的页面来说，1440 是一个偏宽的画幅，而显而易
见的那个修法只会让它更糟。

**第 6 部分是管理员指南，14 页，已作为 `0aa1f92` 推送，尚未对照部署验证。** 四个
决定被提出并回答，其中第一个就是那第十四页。16h 的十三条要点里没有维护开关，而第
5 部分刚刚把那一页改成仅限管理员，于是 8.12 哪里都没有被写到。它发现了**偏离
131**，那是 130 最后一段从另一头走来的样子。四条字串向义工承诺只有管理员才能把他
们的成果发布出去，而实际上任何一个职位发布者都可以。

**第 4 部分给说明文件网站加了一个 service worker，让它可以离线工作。** 它是作为一
个新的第 4 部分出现的，把它下面的一切都往后推了一位。这是这个阶段第二次给自己重新
编号，理由和第 1 部分当初那次一样。在 76 页底下铺管路，在这些页面之前做比在之后做
便宜。三个决定被提出并回答。受限的指南按读者分开缓存，预缓存清单由构建生成，而更
新提示条是求职网站那个模块生成进来的。**它已经在浏览器里跑过**，而这恰恰是这里其
他每一项检查都告诉不了你的事。`--only=install` 会在一个真正的 Chromium 里安装那个
worker，然后把网络拔掉。

**第 5 部分是职位发布者指南，20 页，也是两棵树里第一份受限内容。** 它是从 785 条
`admin.*` 字串和它们背后的路由写出来的，一次坐下来写完。之所以是 20 页，是因为
16h 的十九条要点以登录开头，而一个区块需要一个落地页。四个决定被提出并回答：那第
二十页，以及把 Apps Script 的步骤做成后台那四步的一份**被核对的**副本。另外两个是
为第 8 部分准备的十个截图位置，以及拿它发现的东西怎么办。

**它发现的是偏离 130：一个职位发布者可以把整个职位板关掉。** `/admin/settings` 和
`/admin/maintenance` 就在发布者的侧栏里，而这两条路由都只用 `requireStaff` 把关。
10 item 2 把这两页都点名为管理员的。这个问题有六个阶段那么老。**在这一部分修好
了**，所以第 5 部分像第 2 部分那样动了 `main-site/`。而
`tests/phase7-test.mjs` 一直在用几乎一模一样的话断言这个缺陷。

**第 4a 部分把求职网站的华文名改成：国际兽视入队平台。** 这比英文名更诚实，因为
「Careers」暗示有薪水，而入队说的是加入这支队伍。它移动了 93 条字串、四份文件和
**说明书 3a**，没有 3a 这次改名就不算真的发生。**迁移 `041` 已于 2026 年 9 月 4
日应用**，那是任何词典都够不到的一半：首页的标题是数据库里的一行。

**第 2 部分是求职网站使用指南，十三页，也是两棵树里第一份真正的内容。** 它是从
`en.json` 和那些路由写出来的，而不是从说明书写出来的，这正是第 16 节把这个阶段排
在倒数第二的全部理由。它还发现了一件事：第 5 节第 31 项，首页仍在承诺一个一周前就
已经上线的离线功能。

**接着有人要求做一次平实语言的通读，覆盖两个站点，那就是第 2a 部分。** 简单、简洁
的英文和新加坡华语，写给一个没有技术知识、只想找到一个职位并申请的读者。它的说明
写在下面第 2a 部分底下。它产生的那条规则现在是**说明书 3b**，以及
`check-copy.js` 里的三项检查。一条关于写作、却没有人能运行的规则，是一条有保质期
的规则。

**第 2b 部分是有人在链接预览里发现的一个缺陷：说明文件网站完全没有图标，也没有卡
片。** 没有 `favicon.ico`，没有 `apple-touch-icon`，没有 `og:` 或 `twitter:` 标
记，也没有它自己的任何图片文件。于是标签页显示的是浏览器的空白图标，而分享出去的
链接预览成了一个标题旁边什么都没有。两个 Vercel 项目意味着求职网站的
`/HLC-main.png` 在这里指向不到任何东西。第 2b 部分的说明写在下面，代价是
`gen-docs-lib.js` 里一个新字段。

**第 3 部分是机器人指南和翻译指南，十六页，两者都是公开的。** 这让公开层级达到 30
页并把它写完。指令参考现在是一份被核对的副本。`commands.py --check` 会把它当成第
三份文件来读。另一个站点上的一份指南里的清单，不可能和机器人向 Telegram 注册的那
份清单产生分歧。

**第 13 阶段已于 2026 年 9 月 3 日上线，十五个阶段里有十三个是活的。**
`build-status.json` 读作 `shipped` 并带着两种语言的说明，而第 14 阶段读作
`building`。根目录 README 写着十五分之十三，而 `sw.js` 在 **`v126`**。第 1 节有浓
缩过的说明；第 2 节是第 14 阶段。

**它是按这个文件称为诚实的那个顺序上线的：走查、放开、然后翻转。** 第 5 节第 24
项那次坐下来先发生，`HELLO_WRITES_ENABLED` 在那之后才变成 `true`，而阶段是在那之
后才翻转的。在写下任何东西之前，两个部署都被确认带着这次放开。检查 166 就是让这一
对保持诚实的东西，而它对得起自己的位置。**第一次去问的时候，求职网站还在构建**，
于是它回答 `held`，而说明文件网站回答正常。

**那次走查发现了四件事，而其中每一件对这个代码库的检查都是不可见的。**
`SITE_URL` 从来没有在说明文件的 Vercel 项目上设置过。所以
`/api/auth/staff/account` 和 `/api/auth/staff/passkeys` **从第 6 部分起对每一个员
工账户都回答 500**。`--only=live` 的 27 项检查自始至终都通过，因为它们全都是以陌生
人的身份去问的。gftv.asia 的个人资料链接指向 `/account`，而那是一个带兜底路由的单
页应用，**回答 200**。「在所有设备上退出」让当前浏览器仍然登录着，而且根本没有碰到
gftv.asia。还有**第四个会碰到 gftv.asia 的写入，是那次扣住从来没有覆盖到的**，也
就是重新生成备份代码集。它在代码里四个地方写明过，却在零个地方被数过。第 24、29
和 30 项，偏离 126 和 127。

**其中三件在收尾之后变成了提交**，全部在 2026 年 9 月 3 日推送。**`5270412`** 是
那个链接和检查 167 与 167a，**`c2612c5`** 是那次放开，而 **`643c7dd`** 是退出登录
的修复。**`ea253ce`**，也就是翻转本身，夹在后两者之间。**第 7a、7b、7d 和 7e 部
分，没有 7c** —— 手动重建那些提交时编号跳过了一个。

**翻转落在退出登录的修复之前**，这一点值得知道，只是因为在日志里读起来顺序有点
怪。`ea253ce` 把阶段设成 `shipped`，而 `643c7dd` 在那之后才修好危险区。中间那段时
间部署上没有任何东西是坏的。检查 166 守的是阶段与那次*扣住*之间的关系，而那次扣住
在两个提交之前就已经放开了。

**没有修、而是被写下来的是这一件**：`checkEnv()` 在两个站点上都存在，而没有任何东
西调用它。这正是为什么一条完美的错误讯息在日志里躺了两个星期。第 29 项。

**两个长期挂着的项目结掉了。** 第 26 项：`gftvhello_users.display_name` 和
`.email` 都读得回来，所以第 5a 节里的两个假设是事实，而那条防御性的回退路径从来没
有被走过。第 24 项：员工登录现在已经在笔记本电脑以外的地方跑过了。

**第 13 阶段不欠什么。** 什么都不欠。它自己的「完工前」清单已经清空，而它的检查是
677 项通过，其中二十七项是针对部署的。

**这个构建仍然欠的是第 12 阶段的那些，自 8 月 31 日起没有变化。** 第 4 部分的华文
往返还在评审者手里。一次手动的坐下来涵盖第 10 阶段的设备检查、第 11 阶段的 29 步
清单、第 2 部分那些可交互的管理界面，以及决定 13 那个被关掉的管理页面。**那次坐下
来以 `node seed.mjs --yes --anyway` 开始，以 `--clear --yes` 结束**，决定 27。
`node gen-screenshots.js` 仍然欠着，因为种子被清掉了。

以前放在这个文件开头的第 12 阶段说明照录如下，因为其中没有任何一点发生变化。

**第 12 阶段「打磨」分八个部分上线**：`3bcf34b` 和 `c32a4ab`、`f6c8143`、
`45a3451` 和 `a9478af`、`25028e7`、`a90776c`、`9c1fa73`。然后是 `ce25419`，后面跟
着 `787e21a`、`0c9f219` 和 `1850997`，以及 **`37eecd1`，「phase 12 part 8」**，也
就是收尾。它在 `build-status.json` 里读作 `shipped` 并带着两种语言的说明，而
`sw.js` 在 **`v111`**。第 1 节有这个阶段浓缩过的说明；第 2 节是第 13 阶段。

**有一个提交落在这个阶段之后，且不属于它的任何一部分**：**`2c27a2b`，「phase 12
part 8a」**。它把求职网站的员工会话从 `gftvhello_sessions` 移到这个构建自己拥有的
一张表里。迁移 `038` 在它之前手动应用。它被编号为第 8a 部分，是因为它落在那里，而
不是因为收尾要求它。偏离 122 是完整的说明。**事后两个站点都被登录过一次来证明这件
事**，而这恰恰是这里任何文件都没法自己检查的一件事。

**还有一处改动同样不属于第 12 阶段的任何一部分。** 管理员可以在
`/admin/applicants` 上编辑一个申请人的用户名、电邮、显示名称、电话和语言，并只读地
看到他们的 Telegram id 和用户名。**8.9 里没有这个动作。** 偏离 123 是它的说明。那
里面包括为什么改动一个标识符会让申请人退出登录，而改显示名称不会，以及为什么不通
知任何人。**它作为 `4d338a7`、「phase 12 part 8b」上线。** `16792d4`、`5512c9c`、
`5176fdd`、`4f5f8b9` 和 `ee7bfcd` 跟在它后面，作为第 8c 到 8g 部分。目前还没有任
何东西是对照部署证明过的。已有的检查是 `node check-i18n.js`、`node gen-review.js`
和那些阶段文件，全部干净，而**那个动作本身从来没有跑过**。

**从第 8 部分起，网站对搜索引擎开放**，此前对它们封闭了十一个阶段。
`api/_lib/discovery.js` 里的 `INDEXING` 是 `true`，全局的 `X-Robots-Tag` 已从
`vercel.json` 里去掉，而 `/api/(.*)` 上的那一条保留。两半会互相对照检查，两个方向
都查，所以半成品状态不可能悄悄上线。`--only=discovery-live` 对着带有这次改动的部署
读出 9 项通过、0 项失败、0 项跳过。

**职位板是空的，而这就是正确的职位板**，截至 2026 年 8 月 31 日。开发用的种子在翻
转之后被删掉了，而 `--only=discovery-live` 对着它读出 8 项通过、0 项失败、1 项跳
过。站点地图是那五个静态页面，而数据源回答零个职位。那一项跳过，是那个把两者对照
的检查在明说这一点，而不是拿两个空列表放行。决定 25 是它的论证，而第 5 节第 6 项
是它的代价。

**那次清除还欠着一条命令**：`node gen-screenshots.js`。`manifest.json` 里那两张安
装截图是 `/search` 的真实截图，而它们显示的正是刚刚消失的那九个职位。

**写下那句话时十五个阶段里有十二个是活的**，现在是十三个。第 12 阶段仍然欠的写在
第 5 节里，而其中没有一件是代码。第 4 部分的华文往返还在评审者手里。一次手动的坐
下来涵盖第 10 阶段的设备检查、第 11 阶段的 29 步清单、第 2 部分那些可交互的管理界
面，以及决定 13 那个被关掉的管理页面。**那次坐下来以 `node seed.mjs --yes
--anyway` 开始，以 `--clear --yes` 结束**，决定 27，因为清掉种子会把它要走查的那些
界面清空。

> [!WARNING]
> 是快照，不是实时文件。这一份是在 2026 年 9 月 11 日抓取的。工作副本是
> `reference/next-steps.md`，而它在一个阶段进行期间一天要重写好几次。
> 它会自己往前走而不带上这些页面，所以如果构建还在进行，
> 那个文件就比您正在读的内容更新。

> [!NOTE]
> 这些页面由 `reference/next-steps.md` 通过
> `node gen-memo-pages.js` 生成。在这里做的修改会被下一次运行覆盖掉，
> 并由 `node gen-memo-pages.js --check` 报出来。

## 各小节

| 小节 |
|---|
| [1. 至今完成的](/staff/memo/section-1) |
| [2. 本阶段。第 14 阶段，说明文件。](/staff/memo/section-2) |
| [3. 承重的规则](/staff/memo/section-3) |
| [4. 偏离阶段计划之处，以及原因](/staff/memo/section-4) |
| [5. 继续带着走的，仍未了结](/staff/memo/section-5) |
| [6. 由后续阶段继承的](/staff/memo/section-6) |
| [7. 已定案的决定，以免它们被重新提出](/staff/memo/section-7) |
| [8. 未结项目，都不构成阻碍](/staff/memo/section-8) |
